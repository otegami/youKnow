<% provide(:title, 'Create Task') %>
<% provide(:button_text, 'Create My Task') %>
<% provide(:form_url, project_tasks_path(@project)) %>
<% provide(:method, :post) %>

<h1>Create Task</h1>

<div class="container">
  <div class="row">
    <div class="col-10 col-offset-1 text-left">
      <%= render 'form' %>
    </div>
  </div>
</div>